// to add
// a sorting algorithm to change what's renderd. eg sort by alpha order, size of school, all different things.
// adding in other relevant stats
// searching by name
// maybe a db at some point so I dont have to do API req's

import Landing from '../components/Landing';

export default () => {
  return <Landing />;
};
